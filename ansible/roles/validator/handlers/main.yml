---
- name: restart validator (binary)
  systemd:
    name: validator
    state: restarted
    daemon_reload: true
  when: deploy_mode == "binary"
  listen: restart validator

- name: restart validator (docker)
  docker_container:
    name: validator
    state: started
    restart: true
  when: deploy_mode == "docker"
  listen: restart validator